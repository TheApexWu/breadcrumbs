# Ralph loop notes — M5

Notes left by the autonomous loop for the next iteration. Terse by design.
M0–M4 notes: docs-notes/m0.md–m4.md.

## M5 — done (2026-08-22)

- **agent/comms.py** — the Comms action. Two channels, picked by the M1
  operator alert-preference profile (`profile.channel`):
  - PRIMARY = `TelegramAdapter` (Bot API sendMessage). Token + chat_id from
    env `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` ONLY (never committed; V3
    grep asserts no token literal). `base_url` override (env `TELEGRAM_BASE_URL`
    or ctor) so the adapter is testable against a local relay — no real
    Telegram call needed to prove `ok:true`. Sending is I/O, not inference.
  - FALLBACK = `mock_phone_render()` — writes `bridge/out/alert.json`, ZERO
    network calls. The human-built console reads this static file from the
    local filesystem (no HTTP) so the pull-the-cable demo works offline.
  - `send_alert(response, brief, sms, profile, adapter, out_dir)` dispatches:
    telegram if configured+ok, else falls back to mock phone. Always lands.
  - `build_alert()` — the grounded alert payload (real product, exposed count,
    compounding count, swap target — all traced to the M2 Response).
- **agent/swarm.py** — added `tool_send` (logged to transcript as tool `send`;
    NOT in the M3 canonical 6-tuple, so M3's subsequence check still passes —
    `send` is filtered out by the `if t in CANONICAL` filter). Wired into
    `run()` first-alert path only (dedup path returns `send.skipped=True` —
    no re-send, preserves M3's `alert_count=1` contract). SMS rewritten to be
    <=2 sentences (SMS-style layman alert) per the M5 task.
- **bridge/server.py** — new `/alert?recall=<id>` endpoint + static
  `bridge/out/alert.json` (written by `send_alert` mock-phone fallback during
  `--write-out`). CONTRACT.md documents the endpoint + provenance.
- The alert payload is GROUNDED in the M2 Response — `exposed_count`,
  `compounding_count`, `swap_to`, `recalling_firm`, `product_description`
  all EQUAL `respond()` (asserted by verify_m5 V1).

## Verifications — 17/17 PASS (scripts/verify_m5.py)
- V1: alert.product_description/exposed_count/compounding_count/swap_to/
  recalling_firm == M2 Response; class='grounded'; sms <=2 sentences; sms
  names swap target + exposed count.
- V2a: TelegramAdapter no-token -> clean skip (ok=False, skipped=True,
  network_calls=0). V2b: with test token + local relay (mimics
  /bot<token>/sendMessage -> {ok:true}) -> adapter POSTs, parses ok:true,
  status 200, network_calls=1. V2c: mock_phone_render -> 0 network calls,
  writes alert.json. V2d: profile.channel=console -> mock phone.
- V3: no token literal in repo source (regex scan of agent/sim/db/scripts/
  bridge); no literal `TELEGRAM_BOT_TOKEN=` assignment; adapter reads via
  `os.environ.get(ENV_TOKEN)` with `ENV_TOKEN="TELEGRAM_BOT_TOKEN"` constant;
  `.env` gitignored (`*.env` in .gitignore).

## Gotchas for next iteration (M6 / M7 / M8)
- **M6 on-box**: the Telegram send still works on-box (it's I/O, not
  inference — does not violate local-inference). For the pull-the-cable
  demo, set profile.channel='console' (or unset TELEGRAM_BOT_TOKEN) so the
  alert goes to the mock phone (bridge/out/alert.json) with zero network.
  The live `/alert` endpoint runs the swarm (mutates agent_memory) — clear
  `db.agent_memory` first or use the committed static `bridge/out/alert.json`.
- **Real Telegram send (demo)**: `export TELEGRAM_BOT_TOKEN=<bot token>` and
  `TELEGRAM_CHAT_ID=<operator chat>` before `swarm.run`. The adapter POSTs to
  the real `https://api.telegram.org/bot<token>/sendMessage`. A real message
  lands on a real phone. NEVER commit the token — it's env-only (asserted).
- **tool_send is not in the M3 canonical subsequence** — verify_m3.py filters
  transcript tools to the 6 canonical names, so `send` is invisible to M3's
  assertions. M3 still passes 22/22 with the `send` tool present.
- **`bridge/out/alert.json` is regenerated** by `server.py --write-out`
  (via send_alert mock-phone fallback). Re-run after any M2/M3/M5 change.
- **Test relay pattern**: verify_m5 V2b starts a local ThreadingHTTPServer
  that mimics Telegram's sendMessage and points the adapter at it via
  `base_url`. Reuse this pattern to test any HTTP-based channel without
  real creds.