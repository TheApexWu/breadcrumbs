# Ralph loop notes

Notes left by the autonomous loop for the next iteration. Terse by design.

## M0 — done (2026-08-22)

- **Environment**: default `python3` is 3.9.6 (system) — too old. Created `.venv`
  with `/opt/homebrew/bin/python3.14` (3.14.3). `preflight.sh` bootstraps the venv
  on first run if missing. `mongosh` was installed via `brew install mongosh`
  (was absent); mongod was already running on `localhost:27017` with dbpath
  `/Users/amadeuswoo/dev/mongo-breadcrumbs`.
- **Recalls cache**: there was NO openFDA recalls file in `globe/assets/` (only
  the distributor scorecard + food-types aggregate). Wrote
  `scripts/fetch_recalls.py` which pulls the FULL openFDA food/enforcement
  corpus (29,309 recalls) by slicing on classification (each class < the 25000
  skip cap) and caches to `data/recalls.json` (~34MB, committed). `globe/assets`
  is human-owned so the loop-owned cache lives in `data/`.
- **crit_violations**: `nyc-restaurants.json` is latest-graded only (one
  `critical_flag` string, not a count). Fetched the DOHMH 43nn-pn8j
  `critical_flag='Critical'` count grouped by camis → `data/crit-violations.json`
  (26,386 camis with >=1 critical violation). Merged into each establishment doc
  as `crit_violations:int`. 16,656 of 17,204 establishments have crit_violations>0.
- **Distributor hubs**: geocoded the 5 real NYC-area hubs (Baldor, Restaurant
  Depot, Jetro, Sysco-JerseyCity, US Foods-Bronx) with publicly-known facility
  coords + `loc` GeoJSON Point. Producers (Dole/Fresh Express/Taylor Farms) keep
  real recall history but `loc=null` (they are recalling firms, not hubs).
  `is_hub:true` marks the 5. 2dsphere index on `distributors.loc`.
- **operator_sites**: created EMPTY with its 2dsphere index in M0 so the
  exposure aggregation is runnable now. M1 populates the 12 sites. Until then
  `exposure()` returns [] and the parity check is empty==empty (trivial but
  honest; the real exposure assertion lands in M2).

## Gotchas for next iteration

- openFDA single 1000-row call ~16s; full corpus fetch ~8min. `data/recalls.json`
  is cached so do NOT re-fetch unless refreshing. `fetch_recalls.py` is idempotent.
- `db = db or get_db()` does NOT work — pymongo Database has no `__bool__`. Use
  `db if db is not None else get_db()` (already done in `db/queries.py`).
- The exposure join is `operator_sites.distributor == recall.recalling_firm`.
  Hero recall is DOLE — but operator sites source via NYC hubs (Baldor/etc), not
  DOLE directly. M1/M2 must decide the modeled link (2-hop site→hub→producer, or
  assign produce-category sites a producer distributor). This is THE modeling
  decision for M1; flag it in the brief as MODELED.
- Mongo DB name is `breadcrumbs` (hardcoded in load_mongo.py + queries.py).
- `scripts/verify_m0.py` is the canonical M0 verification runner (16 checks, all
  PASS). Re-run after any reload: `.venv/bin/python scripts/verify_m0.py`.
